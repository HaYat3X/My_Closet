<div id="closet_posts">
    <%# ! header %>
    <div class="pc-header">
        <%= render "coordinates/layouts/pc_header" %>
    </div>

    <div class="row g-0">
        <%# ! sidebar %>
        <div class="col-lg-2-5 pc-sidebar">
            <%= render "coordinates/layouts/pc_sidebar" %>
        </div>

        <%# ! contents %>
        <div class="col-lg-9-5 col-12">
            <div class="list-content">
                <%# * タブ切り替え %>
                <ul class="tab-menu nav nav-underline nav-fill">
                    <li class="nav-item flex-fill">
                        <a href="#all" class="nav-link fw-bold active" data-bs-toggle="tab">ALL</a>
                    </li>

                    <li class="nav-item flex-fill">
                        <a href="#outer" class="nav-link fw-bold" data-bs-toggle="tab">OUTER</a>
                    </li>

                    <li class="nav-item flex-fill">
                        <a href="#tops" class="nav-link fw-bold" data-bs-toggle="tab">TOPS</a>
                    </li>

                    <li class="nav-item flex-fill">
                        <a href="#bottoms" class="nav-link fw-bold" data-bs-toggle="tab">BOTTOMS</a>
                    </li>

                    <li class="nav-item flex-fill">
                        <a href="#shoes" class="nav-link fw-bold" data-bs-toggle="tab">SHOES</a>
                    </li>

                    <li class="nav-item flex-fill">
                        <a href="#other" class="nav-link fw-bold" data-bs-toggle="tab">OTHER</a>
                    </li>
                </ul>

                <div class="tab-content mt-2">
                    <%# ? allタブエリア %>
                    <div id="all" class="tab-pane active">
                        <div class="row g-1">
                            <% @closets_all.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>

                            <%= paginate @closets_all %>
                        </div>
                    </div>

                    <%# ? outerタブエリア %>
                    <div id="outer" class="tab-pane">
                        <div class="row g-1">
                            <% @closets_outer.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>
                        </div>
                    </div>

                    <%# ? topsタブエリア %>
                    <div id="tops" class="tab-pane">
                        <div class="row g-1">
                            <% @closets_tops.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>
                        </div>
                    </div>

                    <%# ? ボトムスタブエリア %>
                    <div id="bottoms" class="tab-pane">
                        <div class="row g-1">
                            <% @closets_pants.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>
                        </div>
                    </div>

                    <%# ? shoesタブエリア %>
                    <div id="shoes" class="tab-pane">
                        <div class="row g-1">
                            <% @closets_shoes.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>
                        </div>
                    </div>

                    <%# ? otherタブエリア %>
                    <div id="other" class="tab-pane">
                        <div class="row g-1">
                            <% @closets_other.each do |c| %>
                                <div class="col-lg-3 mt-1">
                                    <%= link_to "/closet/show/#{c.id}" do %>
                                        <%= image_tag c.photograph.url, class: "closet-image" %>
                                    <% end %>
                                </div>
                            <% end %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>