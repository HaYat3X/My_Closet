<div id="sns_posts">
    <%# ! ヘッダーエリア %>
    <div class="mobile-header">
        <%= render "sns/layouts/mobile_header" %>
    </div>

    <div class="pc-header">
        <%= render "sns/layouts/pc_header" %>
    </div>

    <%# ! サイドバーエリアとコンテンツエリア %>
    <div class="row g-0">
        <%# * サイドバーエリア %>
        <div class="col-lg-2-5 pc-sidebar">
            <%= render "sns/layouts/pc_sidebar" %>
        </div>

        <%# * コンテンツエリア %>
        <div class="col-lg-9-5 col-12">
            <div class="list-content">
                <form id="myForm" class="d-flex justify-content-end" action="/" method="get">
                    <div class="form-check">
                        <%# ? フォロー中のみに切り替える %>
                        <input type="checkbox" class="form-check-input" value="true" id="formSwitchCheckDefault" name="followed_only" <%= params[:followed_only] == 'true' ? 'checked' : '' %>>
                        <label for="formSwitchCheckDefault" class="form-check-label fw-bold">フォロー中のみにする</label>
                    </div>
                </form>

                <ul class="tab-menu nav nav-pills nav-fill col-lg-5">
                    <li class="nav-item">
                        <a href="#all" class="nav-link fw-bold active" data-bs-toggle="tab">全て</a>
                    </li>

                    <li class="nav-item mx-2">
                        <a href="#men" class="nav-link fw-bold" data-bs-toggle="tab">男性</a>
                    </li>

                    <li class="nav-item">
                        <a href="#women" class="nav-link fw-bold" data-bs-toggle="tab">女性</a>
                    </li>
                </ul>

                <div class="tab-content mt-2">
                    <%# ? 全ての投稿を表示する %>
                    <div id="all" class="tab-pane active">
                        <div class="row g-1">
                            <% @snss.each do |sns| %>
                                <div class="col-lg-3 col-md-4 col-4 mt-1">

                                    <%= link_to "/sns/show/#{sns.id}" do %>
                                        <%= image_tag sns.photograph.url, class: "sns-image" %>
                                    <% end %>

                                    <div class="user-area py-2">
                                        <div class="d-flex align-items-center justify-content-between">
                                            <div class="d-flex align-items-center">
                                                <% if sns.user.avatar %>
                                                    <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                        <%= image_tag sns.user.avatar.url %>
                                                    <% end %>
                                                <% else %>
                                                    <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                        <%= image_tag "/assets/default-avatar.png" %>
                                                    <% end %>
                                                <% end %>

                                                <div class="ms-2">
                                                    <div class="fw-bold user-name m-0">
                                                        <%= sns.user.user_name %>
                                                    </div>

                                                    <div class="user-height m-0">
                                                        <%= sns.user.height %>cm
                                                    </div>
                                                </div>
                                            </div>

                                            <div class="mx-2">
                                                <div class="like m-0">
                                                    <% if user_signed_in? %>
                                                        <% if SocialLike.where(social_id: sns.id, user_id: current_user.id).exists? %>
                                                            <%= link_to "/sns/delete_like/#{sns.id}", method: :delete do %>
                                                                <i style="color: #E63758;" class="bi bi-heart-fill"></i>
                                                            <% end %>
                                                        <% else %>
                                                            <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                                <i style="color: #FFFFFF;" class="bi bi-heart"></i>
                                                            <% end %>
                                                        <% end %>
                                                    <% else %>
                                                        <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                            <i class="bi bi-heart fs-5"></i>
                                                        <% end %>
                                                    <% end %>
                                                </div>

                                                <div class="number-of-likes text-center m-0">
                                                    <%= SocialLike.where(social_id: sns.id).count %>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            <% end %>

                            <div class="mt-3">
                                <%= paginate @snss %>
                            </div>
                        </div>
                    </div>

                    <%# ? 男性の投稿のみを表示する %>
                    <div id="men" class="tab-pane">
                        <div class="row g-1">
                            <% @snss.each do |sns| %>
                                <% if sns.user.gender === "男" %>
                                    <div class="col-lg-3 col-md-4 col-4 mt-1">

                                        <%= link_to "/sns/show/#{sns.id}" do %>
                                            <%= image_tag sns.photograph.url, class: "sns-image" %>
                                        <% end %>

                                        <div class="user-area py-2">
                                            <div class="d-flex align-items-center justify-content-between">
                                                <div class="d-flex align-items-center">
                                                    <% if sns.user.avatar %>
                                                        <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                            <%= image_tag sns.user.avatar.url %>
                                                        <% end %>
                                                    <% else %>
                                                        <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                            <%= image_tag "/assets/default-avatar.png" %>
                                                        <% end %>
                                                    <% end %>

                                                    <div class="ms-2">
                                                        <div class="fw-bold user-name m-0">
                                                            <%= sns.user.user_name %>
                                                        </div>

                                                        <div class="user-height m-0">
                                                            <%= sns.user.height %>cm
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="mx-2">
                                                    <div class="like m-0">
                                                        <% if user_signed_in? %>
                                                            <% if SocialLike.where(social_id: sns.id, user_id: current_user.id).exists? %>
                                                                <%= link_to "/sns/delete_like/#{sns.id}", method: :delete do %>
                                                                    <i style="color: #E63758;" class="bi bi-heart-fill"></i>
                                                                <% end %>
                                                            <% else %>
                                                                <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                                    <i style="color: #FFFFFF;" class="bi bi-heart"></i>
                                                                <% end %>
                                                            <% end %>
                                                        <% else %>
                                                            <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                                <i class="bi bi-heart fs-5"></i>
                                                            <% end %>
                                                        <% end %>
                                                    </div>

                                                    <div class="number-of-likes text-center m-0">
                                                        <%= SocialLike.where(social_id: sns.id).count %>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% end %>
                            <% end %>

                            <div class="mt-3">
                                <%= paginate @snss %>
                            </div>
                        </div>
                    </div>

                    <%# ? 女性の投稿のみを表示する %>
                    <div id="women" class="tab-pane">
                        <div class="row g-1">
                            <% @snss.each do |sns| %>
                                <% if sns.user.gender === "女" %>
                                    <div class="col-lg-3 col-md-4 col-4 mt-1">

                                        <%= link_to "/sns/show/#{sns.id}" do %>
                                            <%= image_tag sns.photograph.url, class: "sns-image" %>
                                        <% end %>

                                        <div class="user-area py-2">
                                            <div class="d-flex align-items-center justify-content-between">
                                                <div class="d-flex align-items-center">
                                                    <% if sns.user.avatar %>
                                                        <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                            <%= image_tag sns.user.avatar.url %>
                                                        <% end %>
                                                    <% else %>
                                                        <%= link_to "/profile/show/#{sns.user_id}" do %>
                                                            <%= image_tag "/assets/default-avatar.png" %>
                                                        <% end %>
                                                    <% end %>

                                                    <div class="ms-2">
                                                        <div class="fw-bold user-name m-0">
                                                            <%= sns.user.user_name %>
                                                        </div>

                                                        <div class="user-height m-0">
                                                            <%= sns.user.height %>cm
                                                        </div>
                                                    </div>
                                                </div>

                                                <div class="mx-2">
                                                    <div class="like m-0">
                                                        <% if user_signed_in? %>
                                                            <% if SocialLike.where(social_id: sns.id, user_id: current_user.id).exists? %>
                                                                <%= link_to "/sns/delete_like/#{sns.id}", method: :delete do %>
                                                                    <i style="color: #E63758;" class="bi bi-heart-fill"></i>
                                                                <% end %>
                                                            <% else %>
                                                                <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                                    <i style="color: #FFFFFF;" class="bi bi-heart"></i>
                                                                <% end %>
                                                            <% end %>
                                                        <% else %>
                                                            <%= link_to "/sns/create_like/#{sns.id}", method: :post do %>
                                                                <i class="bi bi-heart fs-5"></i>
                                                            <% end %>
                                                        <% end %>
                                                    </div>

                                                    <div class="number-of-likes text-center m-0">
                                                        <%= SocialLike.where(social_id: sns.id).count %>
                                                    </div>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                <% end %>
                            <% end %>

                            <div class="mt-3">
                                <%= paginate @snss %>
                            </div>
                        </div>
                    </div>  
                </div>
            </div>
        </div>
    </div>
</div>