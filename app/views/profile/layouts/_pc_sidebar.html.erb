<div class="sidebar d-flex flex-column flex-shrink-0 sticky-top vh-100">
    <ul class="nav nav-pills flex-column">
        <span class="fs-4 pt-3 px-5">メニュー</span>

        <li class="nav-item pt-2">
            <a href="/" class="d-flex align-items-center px-5 py-1" aria-current="page">
                <i class="bi bi-chat"></i>
                SNS
            </a>
        </li>

        <li class="nav-item">
            <a href="/closet/list" class="d-flex align-items-center px-5 py-1">
                <i class="bi bi bi-grid"></i>
                クローゼット
            </a>
        </li>

        <li class="nav-item">
            <a href="/suggestion" class="d-flex align-items-center px-5 py-1">
                <i class="bi bi-robot"></i>
                AI提案
            </a>
        </li>

        <li class="nav-item">
            <%# * ユーザーがログインしているかの判定をする %>
            <% if user_signed_in? %>
                <%= link_to "/profile/show/#{current_user.id}", class: "d-flex align-items-center px-5 py-1 active" do %>
                    <i class="bi bi-person-circle"></i>
                    マイページ
                <% end %>
            <% else %>
                <%= link_to "/profile/show/1", class: "d-flex align-items-center px-5 py-1 active" do %>
                    <i class="bi bi-person-circle"></i>
                    マイページ
                <% end %>
            <% end %>
        </li>

        <li class="nav-item">
            <a href="/profile/alert/list" class="d-flex align-items-center px-5 py-1">
                <i class="bi bi-bell"></i>
                通知
            </a>
        </li>

        <hr class="mx-4">

        <label class="fs-4 px-5">その他</label>

        <li class="nav-item">
            <a href="/contact/new" class="d-flex align-items-center px-5">
                <i class="bi bi-box-arrow-up-right"></i>
                お問い合わせ
            </a>
        </li>
        
        <li class="nav-item pb-5">
            <%# * ユーザーがログインしているかの判定をする %>
            <% if user_signed_in? %>
                <%= link_to destroy_user_session_path, method: :delete, class: "d-flex align-items-center px-5" do %>
                    <i class="bi bi-person-circle"></i>
                    サインアウト
                <% end %>
            <% else %>
                <%= link_to new_user_session_path, method: :get, class: "d-flex align-items-center px-5" do %>
                    <i class="bi bi-person-circle"></i>
                    サインイン
                <% end %>
            <% end %>
        </li>
    </ul>
</div>